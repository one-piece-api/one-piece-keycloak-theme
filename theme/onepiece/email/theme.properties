# Tema email "onepiece": eredita struttura e tutti i .ftl per-messaggio dal
# tema ufficiale "base" - sostituisce solo il layout condiviso (html/template.ftl,
# che nel tema base e' un wrapper vuoto <html><body>) e la copy (messages/) per
# it/en, stessa logica del tema login (vedi login/theme.properties): nessun
# .ftl per-messaggio (executeActions.ftl, password-reset.ftl, ...) viene
# copiato o modificato, quindi il tema resta allineato ai futuri aggiornamenti
# di Keycloak sullo stesso major (26.x).
parent=base
